<% books.forEach(function(book) { %>
      <%= book.bookname %>
      <%= book.price %>
      <%= book.arthur %>
      <%= book.img %>

      <img src = "<%='book.img'%>" class="responsive-img">
      
     
      
<% }); %>  